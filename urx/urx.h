#pragma once

#include <cstdint>

namespace urx {

constexpr const uint16_t URX_VERSION_MAJOR(1);
constexpr const uint16_t URX_VERSION_MINOR(0);
constexpr const uint16_t URX_VERSION_PATCH(0);

}  // namespace urx
